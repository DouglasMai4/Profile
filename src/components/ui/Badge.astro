---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<span
	class:list={[
		'inline-flex items-center px-4 py-1.5 text-sm font-medium rounded-full border bg-primary/10 text-primary border-primary/20 backdrop-blur-md',
		className,
	]}
>
	<slot />
</span>
